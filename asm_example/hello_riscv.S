# File, generated by AI

# hello_riscv.s
# Hello World на RISC-V для Linux

.data
msg:
    .string "Hello, World!\n"
    len = . - msg

.text
.globl main

main:
    # Системный вызов write(1, msg, len)
    li a7, 64           # номер системного вызова write (64)
    li a0, 1            # файловый дескриптор stdout (1)
    la a1, msg          # адрес строки
    li a2, len          # длина строки
    ecall               # вызов системы

    # Системный вызов exit(0)
    li a7, 93           # номер системного вызова exit (93)
    li a0, 0            # код возврата 0
    ecall               # вызов системы
